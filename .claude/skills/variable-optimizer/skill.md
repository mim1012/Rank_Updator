---
name: variable-optimizer
description: 변수 조합 최적화 자동화 (유전 알고리즘)
---

# Variable Optimizer Skill

12개 변수 조합을 유전 알고리즘으로 자동 최적화하는 스킬입니다.

## 사용 시나리오

1. **매일 자정 자동 진화**
2. **성능 하락 감지 시 새 탐색**
3. **캠페인 시작 전 최적 조합 제공**

## 자동 실행 워크플로우

```
1. 현재 세대 성능 평가
   ↓
2. 엘리트 조합 선택 (상위 10%)
   ↓
3. 새 세대 생성
   - 엘리트 유지: 10%
   - 교차: 40%
   - 변이: 40%
   - 랜덤: 10%
   ↓
4. 테스트 실행 (각 조합 10회)
   ↓
5. 성능 점수 계산
   ↓
6. 등급 분류 (Elite/Significant/Deprecated)
   ↓
7. docs/dashboard.md 업데이트
   ↓
8. Orchestrator에게 결과 보고
```

## 실행 방법

```bash
# 스킬 실행
/skill variable-optimizer "세대 5 진화 및 테스트"

# 또는 슬래시 커맨드 사용
/evolve --generation 5
```

## 성능 점수 공식

```
Score = (0.5 × 순위점수) + (0.3 × 성공률) + (0.2 × 캡처회피율)

순위점수 = 1 - (평균순위 / 1000)
성공률 = 성공횟수 / 전체시도횟수
캡처회피율 = 캡처회피횟수 / 전체시도횟수
```

## 환경 변화 감지

```typescript
// 최근 7일간 엘리트 성능 20% 이상 하락 시
if (performanceDropRate > 0.2) {
  // 새로운 탐색 시작
  startNewExploration();
}
```

## 출력 예시

```
✅ 세대 5 진화 완료

📊 결과:
- Elite: 5개 (평균 점수: 8,500)
- Significant: 20개 (평균 점수: 6,200)
- Deprecated: 25개 (평균 점수: 3,800)

🏆 최고 성능 조합 (ID: 247):
- Performance Score: 9,200
- 평균 순위: 15위
- 성공률: 96%
- 캡처 회피율: 98%

⏭️ 다음 단계:
- 세대 6 진화 예정: 내일 자정
- 캠페인에 자동 적용: 조합 ID 247
```

## 관련 코드

- `server/services/variableCombinations.ts`: 핵심 로직
- `server/services/variableConverter.ts`: AdPang 변환
- `agents/variable_agent.md`: Agent 정의
