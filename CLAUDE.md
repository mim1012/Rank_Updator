# Turafic AI Agentic System Guide

**프로젝트**: Turafic (네이버/쿠팡 순위 최적화 AI 시스템)
**버전**: v0.2.0-integrated
**작성일**: 2025-11-16

---

## 1. 프로젝트 개요

Turafic은 **완전 자율 운영되는 AI Agentic 시스템**으로, 네이버/쿠팡 쇼핑몰 순위를 자동으로 최적화하는 플랫폼입니다.

### 핵심 원칙

1. **완전 자율성**: 사람의 개입 없이 모든 의사결정을 AI가 수행
2. **지속적 학습**: 모든 작업 결과를 학습하여 전략 개선
3. **분산 의사결정**: 5개 전문 Agent가 협력하여 최적화
4. **적응적 행동**: 플랫폼 알고리즘 변화에 실시간 대응

---

## 2. AI Agent 시스템

### 2.1 Agent 구조

```
┌─────────────────────────────────────────────────────────┐
│              Orchestrator Agent (중앙 조율자)             │
│  - 전체 시스템 상태 모니터링                               │
│  - Agent 간 작업 조율                                     │
│  - 목표 달성 여부 평가                                     │
└─────────────────────────────────────────────────────────┘
                           │
        ┌──────────────────┼──────────────────┬──────────┐
        │                  │                  │          │
┌───────▼──────┐  ┌────────▼─────┐  ┌────────▼─────┐  ┌▼────────┐
│ Variable     │  │ Bot          │  │ Campaign     │  │Analysis │
│ Agent        │  │ Agent        │  │ Agent        │  │Agent    │
│              │  │              │  │              │  │         │
│ - 변수 조합   │  │ - 봇 상태    │  │ - 캠페인     │  │- 순위   │
│   최적화     │  │   모니터링   │  │   전략 수립   │  │  분석   │
│ - A/B 테스트 │  │ - 작업 할당  │  │ - 실행 제어  │  │- 패턴   │
│ - 유전       │  │ - 장애 복구  │  │ - 예산 관리  │  │  인식   │
│   알고리즘   │  │              │  │              │  │         │
└──────────────┘  └──────────────┘  └──────────────┘  └─────────┘
```

### 2.2 Agent 정의 위치

모든 Agent는 `agents/` 폴더에 Markdown 파일로 정의:

```
agents/
├── orchestrator.md        # 중앙 조율자
├── variable_agent.md      # 변수 최적화 에이전트
├── bot_agent.md           # 봇 관리 에이전트
├── campaign_agent.md      # 캠페인 관리 에이전트
└── analysis_agent.md      # 분석 에이전트
```

### 2.3 Agent 사용 방법

```bash
# Agent 호출 예시 (Claude Code에서)
/agents orchestrator "현재 시스템 상태 점검 및 보고"
/agents variable "새로운 세대 진화 실행"
/agents bot "오프라인 봇 3대 복구"
```

---

## 3. 프로젝트 구조 규칙

### 3.1 폴더 구조

```
turafic/
├── .claude/
│   ├── commands/          # 슬래시 커맨드
│   ├── skills/            # Claude 스킬
│   └── settings.local.json
├── agents/                # AI Agent 정의
├── docs/                  # 모든 문서
│   ├── prd/              # 요구사항 문서
│   ├── architecture/     # 아키텍처 설계
│   ├── api_specs/        # API 명세
│   ├── reverse_engineering/  # 역공학 (AdPang)
│   └── dashboard.md      # 실시간 대시보드
├── server/
│   ├── services/         # 비즈니스 로직
│   │   ├── variableCombinations.ts
│   │   ├── variableConverter.ts
│   │   └── botManager.ts
│   └── routers.ts        # tRPC API
├── client/               # 프론트엔드
└── src/
    ├── frida/           # Frida 후킹 스크립트
    └── automation/      # 자동화 스크립트
```

### 3.2 파일 참조 규칙

- `@` 멘션 사용: `@docs/prd/variable_optimization.md`
- 경로는 항상 프로젝트 루트 기준

---

## 4. 실시간 모니터링 대시보드

### 4.1 위치

`docs/dashboard.md`

### 4.2 업데이트 규칙

- **초기화**: 프로젝트 시작 시 기본 구조 설정
- **자동 업데이트**: Agent 작업 시작/완료 시 실시간 갱신
- **내용**:
  - [프로젝트 목표]
  - [전체 진행률]
  - [Agent별 현재 상태]
  - [최근 활동 로그]
  - [다음 단계]

### 4.3 업데이트 예시

```markdown
## 👥 Agent별 현재 상태

| Agent | 상태 | 현재 작업 | 완료율 |
|---------|------|-----------|--------|
| **Orchestrator** | 🟢 활성 | 시스템 점검 | 100% |
| **Variable Agent** | 🟢 활성 | 세대 3 진화 중 | 75% |
| **Bot Agent** | 🟡 경고 | 봇 2대 오프라인 | 91% |
| **Campaign Agent** | 🟢 활성 | 캠페인 5개 실행 중 | 100% |
| **Analysis Agent** | 🟢 활성 | 순위 분석 중 | 85% |
```

---

## 5. 슬래시 커맨드

### 5.1 정의 위치

`.claude/commands/` 폴더

### 5.2 주요 커맨드

| 커맨드 | 설명 | 예시 |
|--------|------|------|
| `/campaign` | 캠페인 관리 | `/campaign start "갤럭시 S24"` |
| `/bot` | 봇 관리 | `/bot status` |
| `/test` | A/B 테스트 실행 | `/test generation 3` |
| `/analyze` | 순위 분석 | `/analyze campaign 1` |
| `/evolve` | 변수 진화 | `/evolve --generation 4` |

### 5.3 커맨드 생성

```markdown
---
name: campaign
description: 캠페인 생성, 시작, 중지, 분석
---

캠페인을 관리합니다.

사용법:
- /campaign list: 모든 캠페인 조회
- /campaign start <id>: 캠페인 시작
- /campaign stop <id>: 캠페인 중지
- /campaign analyze <id>: 캠페인 성과 분석
```

---

## 6. Claude Skills

### 6.1 정의 위치

`.claude/skills/` 폴더

### 6.2 주요 스킬

| 스킬 | 파일 | 목적 |
|------|------|------|
| **variable-optimizer** | `variable-optimizer/` | 변수 조합 최적화 자동화 |
| **bot-manager** | `bot-manager/` | 봇 네트워크 관리 자동화 |
| **campaign-executor** | `campaign-executor/` | 캠페인 실행 및 모니터링 |
| **rank-analyzer** | `rank-analyzer/` | 순위 변동 분석 |

### 6.3 스킬 호출

```bash
# Claude Code에서
/skill variable-optimizer "세대 5 진화 및 테스트"
/skill bot-manager "모든 봇 상태 점검 및 복구"
```

---

## 7. 자동화된 버전 관리

### 7.1 커밋 단위

각 Agent는 **의미 있는 작업 단위**가 완료될 때마다 커밋:

```bash
git commit -m "feat(VariableAgent): Generate generation 5 - 50 combinations
- Elite: 5개 유지
- Crossover: 20개 생성
- Mutation: 20개 생성
- Random: 5개 생성

Performance Score 평균: 7,500 → 8,200 (+9.3%)

🤖 Generated with Claude Code
Co-Authored-By: Claude <noreply@anthropic.com>"
```

### 7.2 커밋 메시지 규칙

```
<type>(<agent>): <subject>

<body>

🤖 Generated with Claude Code
Co-Authored-By: Claude <noreply@anthropic.com>
```

**Type**:
- `feat`: 새 기능
- `fix`: 버그 수정
- `refactor`: 리팩토링
- `test`: 테스트
- `docs`: 문서

**Agent**:
- `Orchestrator`
- `VariableAgent`
- `BotAgent`
- `CampaignAgent`
- `AnalysisAgent`

---

## 8. 피드백 및 수정 루프

### 8.1 버그 리포팅

```markdown
# Bug Report: 봇 재시작 실패

**발생 시각**: 2025-11-16 14:30:00
**Agent**: BotAgent
**심각도**: 높음

## 증상
- 봇 ID 5, 7, 9가 3회 재시작 시도 후에도 온라인 복구 실패

## 원인
- IP 차단 감지 (403 Forbidden)

## 해결 방안
- 대장봇 재시작하여 IP 변경
- 해당 봇들을 새 그룹으로 재할당

## 담당 Agent
- BotAgent → 즉시 수정
```

### 8.2 자동 재할당

Orchestrator가 버그 리포트를 감지하면 해당 Agent에게 자동으로 수정 작업 할당.

---

## 9. 변수 시스템 (12개)

### 9.1 통합 변수

```typescript
// Turafic 기존 (10개)
1. user_agent: ['UA58', 'UA67', 'UA71']
2. cw_mode: ['CW해제', 'CW유지']
3. entry_point: ['쇼핑DI', '광고DI', '통합검색']
4. cookie_strategy: ['로그인쿠키', '비로그인쿠키']
5. image_loading: ['이미지패스', '이미지로드']
6. input_method: ['복붙', '타이핑']
7. random_clicks: [0, 3, 6]
8. more_button: ['더보기패스', '더보기클릭']
9. x_with_header: ['x-with삼성', 'x-with갤럭시']
10. delay_mode: ['딜레이감소', '딜레이정상']

// AdPang 신규 (2개)
11. work_type: ['검색만', '검색+클릭', '검색+클릭+체류', '리뷰조회']
12. sec_fetch_site_mode: ['same-origin', 'same-site', 'cross-site', 'none']
```

### 9.2 변수 변환

```typescript
// AdPang ↔ Turafic 변환
import { convertAdPangToTurafic, convertTuraficToAdPang }
  from './server/services/variableConverter';
```

---

## 10. 개발 워크플로우

### 10.1 새 기능 개발

```bash
# 1. Agent 정의 확인
cat agents/variable_agent.md

# 2. 슬래시 커맨드로 작업 시작
/evolve --generation 5

# 3. Variable Agent가 자동으로:
#    - 엘리트 선택
#    - 교차 및 변이
#    - 새 세대 생성
#    - 테스트 실행

# 4. 결과 확인
cat docs/dashboard.md

# 5. 커밋
git commit -m "feat(VariableAgent): ..."
```

### 10.2 버그 수정

```bash
# 1. 버그 리포트 작성
# docs/bugs/bug_<timestamp>.md

# 2. Orchestrator가 자동 감지 및 재할당

# 3. 해당 Agent가 수정

# 4. 테스트 및 검증

# 5. 커밋
git commit -m "fix(BotAgent): ..."
```

---

## 11. 참고 문서

- [통합 요약](docs/INTEGRATION_SUMMARY.md)
- [변수 매핑](docs/VARIABLE_MAPPING.md)
- [AI Agentic System Design](AI_AGENTIC_SYSTEM_DESIGN.md)
- [Test Evaluation System](TEST_EVALUATION_SYSTEM.md)
- [Roadmap](ROADMAP.md)

---

**작성자**: Claude Code
**최종 수정일**: 2025-11-16
